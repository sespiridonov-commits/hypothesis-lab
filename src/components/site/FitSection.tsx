import { Check,X } from "lucide-react"; import { Section } from "./Section";
const FIT=["у вас уже есть понятный продукт","есть сайт или посадочная","есть бюджет на тестирование рекламы","готовы проверять гипотезы"];const NOT=["нужен только технический запуск","нужен «Директ максимально дёшево»","нужна гарантия заявок"];
export function FitSection(){return <Section id="fit" label="совместимость" title="Подхожу не каждому бизнесу — и это нормально"><div className="mt-12 grid gap-4 md:grid-cols-2"><div className="rounded-2xl border border-accent bg-accent-soft p-6"><p className="label-mono text-accent">Подойду, если</p>{FIT.map(x=><p key={x} className="mt-4 flex gap-3"><Check className="size-4 text-accent"/>{x}</p>)}</div><div className="rounded-2xl border border-border bg-card p-6"><p className="label-mono">Не подойду, если</p>{NOT.map(x=><p key={x} className="mt-4 flex gap-3 text-muted-foreground"><X className="size-4"/>{x}</p>)}</div></div></Section>}