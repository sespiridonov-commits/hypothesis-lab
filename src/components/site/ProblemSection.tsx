import { Section,Reveal } from "./Section"; import { cn } from "@/lib/utils";
const CLASSIC=["собрать ключи","написать объявления","запустить","ждать"]; const MINE=["разобраться в продукте","найти сегменты","определить интенты","сформировать гипотезы","выбрать приоритетные связки","протестировать","масштабировать победителей"];
function Column({title,items,accent}:{title:string;items:string[];accent?:boolean}){return <div className={cn("rounded-2xl border p-6 sm:p-8",accent?"border-accent bg-accent-soft shadow-card":"border-border bg-card")}><p className={cn("label-mono",accent&&"text-accent")}>{title}</p><ul className="mt-6 space-y-3">{items.map((item,i)=><li key={item} className="flex items-baseline gap-3 text-[15px] text-ink"><span className="label-mono">{String(i+1).padStart(2,"0")}</span>{item}</li>)}</ul></div>}
export function ProblemSection(){return <Section id="problem" label="проблема" title="Проблема часто не в рекламном кабинете" subtitle="Можно идеально настроить кампанию и всё равно получать слабый результат. Если неправильно определён сегмент, выбран не тот рекламный угол или человеку показывают предложение, которое не отвечает его реальному намерению — дополнительный бюджет проблему не решит."><div className="mt-12 grid gap-5 md:grid-cols-2"><Reveal><Column title="Классический подход" items={CLASSIC}/></Reveal><Reveal delay={90}><Column title="Мой подход" items={MINE} accent/></Reveal></div></Section>}