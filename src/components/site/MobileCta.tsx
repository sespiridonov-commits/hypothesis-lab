import { useEffect,useState } from "react"; import { cn } from "@/lib/utils"; import { track } from "@/lib/analytics"; export function MobileCta(){const[show,setShow]=useState(false);useEffect(()=>{const f=()=>setShow(window.scrollY>600);f();window.addEventListener("scroll",f,{passive:true});return()=>window.removeEventListener("scroll",f)},[]);return <div className={cn("fixed inset-x-0 bottom-0 z-40 border-t border-border bg-background/90 p-3 backdrop-blur-xl transition-transform md:hidden",show?"translate-y-0":"translate-y-full")}><a href="#lead" onClick={()=>track("hero_cta_click",{location:"mobile_sticky"})} className="flex w-full justify-center rounded-full bg-accent px-6 py-3.5 text-accent-foreground">Получить 3 гипотезы</a></div>}