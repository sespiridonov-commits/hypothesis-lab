import { useEffect,useState } from "react";
import { Menu,X } from "lucide-react";
import { cn } from "@/lib/utils";
import { track } from "@/lib/analytics";
const NAV=[{href:"#approach",label:"Подход"},{href:"#cases",label:"Кейсы"},{href:"#services",label:"Что делаю"},{href:"#about",label:"Обо мне"}];
export function Header(){const[compact,setCompact]=useState(false);const[open,setOpen]=useState(false);useEffect(()=>{const onScroll=()=>setCompact(window.scrollY>24);onScroll();window.addEventListener("scroll",onScroll,{passive:true});return()=>window.removeEventListener("scroll",onScroll)},[]);return <header className={cn("sticky top-0 z-50 border-b transition-all",compact?"border-border bg-background/85 backdrop-blur-xl":"border-transparent bg-background")}><div className="mx-auto flex w-full max-w-6xl items-center justify-between px-5 py-4 sm:px-8"><a href="#top"><span className="text-[15px] font-medium text-ink">Сергей Спиридонов</span></a><nav className="hidden items-center gap-8 md:flex">{NAV.map(i=><a key={i.href} href={i.href} className="text-sm text-muted-foreground hover:text-ink">{i.label}</a>)}</nav><div className="flex items-center gap-2"><a href="#lead" onClick={()=>track("hero_cta_click",{location:"header"})} className="hidden rounded-full bg-ink px-5 py-2.5 text-sm font-medium text-primary-foreground md:inline-flex">Получить разбор</a><button aria-label={open?"Закрыть меню":"Открыть меню"} onClick={()=>setOpen(v=>!v)} className="inline-flex size-10 items-center justify-center rounded-full border border-border md:hidden">{open?<X className="size-5"/>:<Menu className="size-5"/>}</button></div></div>{open&&<nav className="border-t border-border bg-background px-5 py-3 md:hidden">{NAV.map(i=><a key={i.href} href={i.href} onClick={()=>setOpen(false)} className="block border-b border-border py-4 text-lg">{i.label}</a>)}</nav>}</header>}