import { useState } from "react"; import { Plus } from "lucide-react"; import { Section } from "./Section"; import { cn } from "@/lib/utils";
const ITEMS=[{q:"Вы только настраиваете рекламу?",a:"Нет. Настройка рекламного кабинета — только часть работы."},{q:"С какими рекламными каналами работаете?",a:"В первую очередь Яндекс Директ, РСЯ и VK Ads."},{q:"Можно ли работать, если реклама уже запущена?",a:"Да. Можно начать с разбора текущей рекламы."},{q:"Работаете только с одной нишей?",a:"Нет. Один из моих сильных навыков — быстро разбираться в незнакомом продукте."},{q:"Есть ли гарантии заявок?",a:"Нет. Конкретное количество заявок до тестирования честно гарантировать нельзя."}];
export function FAQ(){const[open,setOpen]=useState<number|null>(0);return <Section id="faq" label="faq" title="Частые вопросы"><div className="mt-10 border-t border-border">{ITEMS.map((x,i)=><div key={x.q} className="border-b border-border"><button onClick={()=>setOpen(open===i?null:i)} className="flex w-full justify-between py-6 text-left"><span className="text-lg font-medium">{x.q}</span><Plus className={cn("size-4",open===i&&"rotate-45 text-accent")}/></button>{open===i&&<p className="pb-6 text-muted-foreground">{x.a}</p>}</div>)}</div></Section>}