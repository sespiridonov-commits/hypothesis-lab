import type { ReactNode } from "react";
import { cn } from "@/lib/utils";
import { useReveal } from "@/hooks/use-reveal";
export function Reveal({children,className,delay=0,as:Tag="div"}:{children:ReactNode;className?:string;delay?:number;as?:"div"|"li"|"section"|"article"}){const {ref,shown}=useReveal<HTMLDivElement>();return <Tag ref={ref as never} style={{transitionDelay:`${delay}ms`}} className={cn("reveal",shown&&"reveal-in",className)}>{children}</Tag>}
export function Section({id,label,title,subtitle,children,className,bordered=true}:{id?:string;label?:string;title?:ReactNode;subtitle?:ReactNode;children?:ReactNode;className?:string;bordered?:boolean}){return <section id={id} className={cn("scroll-mt-24 px-5 py-20 sm:px-8 md:py-28",bordered&&"border-t border-border",className)}><div className="mx-auto w-full max-w-6xl">{(label||title||subtitle)&&<Reveal className="max-w-3xl">{label&&<p className="label-mono mb-4">{label}</p>}{title&&<h2 className="text-[clamp(1.75rem,4.4vw,3rem)] leading-[1.05] font-medium text-ink">{title}</h2>}{subtitle&&<p className="mt-4 max-w-2xl text-base text-muted-foreground sm:text-lg">{subtitle}</p>}</Reveal>}{children}</div></section>}